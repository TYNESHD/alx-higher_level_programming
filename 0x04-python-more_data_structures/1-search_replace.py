#!/usr/bin/python3

def search_replace(my_list, search, replace):
    my_list = new_list
    for x in new_list:
        if new_list[x] == search:
            return new_list[replace]
        else:
            return new_list[x]
